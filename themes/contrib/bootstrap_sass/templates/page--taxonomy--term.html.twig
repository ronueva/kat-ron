<div id="full_page" class="w-100 h-100 m-0 p-0">
  <div id="hero" class="{{ container }} py-0 px-4 px-sm-5 vh-100 section">
    {{ hero }}
    <div class="scroll-down"></div>
  </div>
  <div id="title-area" class="{{ container }} pt-5 px-4 min-vh-100 h-100 section">
    <div class="row h-100">
      <div class="col-12 col-lg-4 mt-sm-0 order-lg-1">
        {{ page.featured_top }}
      </div>
      <div class="col-12 col-lg-4 mt-5 mt-sm-0 order-lg-0 text-lg-start text-align-center">
        {{ page.featured_bottom_first }}
      </div>
      <div class="col-12 col-lg-4 mt-sm-0 order-lg-2 text-lg-end text-align-center">
        {{ page.featured_bottom_second }}
      </div>
    </div>
  </div>
  <div id="entourage" class="py-5 px-4 px-sm-5 mt-5 min-vh-100 h-100 section">
    <div class="row w-100 h-100 my-auto mx-0 px-0">
      <div class="col-12 col-sm-12 d-flex mt-sm-0">
        <h2>
          Entourage
        </h2>
      </div>
    </div>
    <div class="row w-100 h-100 my-auto mx-0 mt-4">
      {% for item in entourage %}
        <div class="col-12 col-sm-4 d-flex mt-sm-0 flex-column py-4 px-2">
          <h5><b>{{ item|first }}</b></h5>
          {% for detail in item[1:] %}
            {% if item|first == "Secondary Sponsors" %}
              <p><i>{{ detail|first }}</i></p>
              {% for subdetail in detail[1:] %}
                <p>{{ subdetail|first }}</p>
              {% endfor %}
            {% else %}
              <p>{{ detail|first }}</p>
            {% endif %}
          {% endfor %}
        </div>
      {% endfor %}
    </div>
  </div>
  <div id="carousel" class="{{ container }} p-0 mb-0">
    {{ page.featured_bottom_third }}
  </div>

  <div class="py-5 px-4 px-sm-5 h-100" id="accommodation">
    <div class="row w-100 h-100 my-auto mx-0">
      <div class="col-12 col-sm-12 d-flex mt-sm-0 flex-column">
        <h2>
          Accommodation
        </h2>
        <p class="mt-3">There are a variety of accommodation options in the surrounding area including hotels, resorts, and Airbnb. Please find some of the top recommendations from our venue below.</p>
      </div>
    </div>
    <div class="row w-100 h-100 mt-5 mx-0 accom-carousel">
      <div class="col-12 d-flex flex-column text-align-center px-0 px-md-1">
        {{ page.featured_bottom_fourth }}
      </div>
    </div>
  </div>
  <div id="rsvp" class="{{ container }} pt-5 pb-5 px-4 px-sm-5 min-vh-100 h-100 section d-flex">
    <div class="row w-100 h-100 my-auto mx-0">
      <div class="col-12 col-sm-6 d-flex mt-sm-0">
        <div class="d-block mt-auto mb-auto">
          <h2 class="rsvp-title">RSVP</h2>
          <p class="fs-6 mt-3 pe-sm-5">As much as we love to celebrate with everyone, due to budget and space limitations, we simply can’t afford for all of our lovely guests to bring plus ones.<br><br>
            Therefore, we regretfully our guests to please not bring a plus one unless they are specifically named on the invitation. Thank you so much for understanding!<br><br>
            We can’t wait to celebrate with you all!<br><br>
            <i>Kindly respond by march 11th</i>
          </p>
        </div>
      </div>
      <div class="col-12 col-sm-6 d-flex mt-sm-0 mt-5">
        <div class="mt-auto mb-auto w-100 h-100">
          {{ page.content }}
        </div>
      </div>
    </div>
  </div>
  <footer class="d-flex flex-column section vh-100">
    <div id="map"></div>
    <div class="p-5 mt-auto">
      {{ page.footer_first }}
    </div>
  </footer>
</div>
